name: lobster
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.2
  charts_flutter: ^0.9.0
  crypto: ^2.1.5
  shared_preferences: ^0.5.12+4


dev_dependencies:
  flutter_launcher_icons: ^0.8.1
  flutter_test:
    sdk: flutter
  #integration_test:
  #  sdk: flutter
  
flutter_icons:
  android: true
  image_path: "assets/lobster.png"
  

flutter:
  uses-material-design: true